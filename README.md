# Naluri Space App

## Getting started
1. Install packages
```bash
pnpm install
```

2. Start dev server
```bash
pnpm run dev
```

## Environment Variables
- To use env in vite, replace `process.env.NODE_ENV` with `import.meta.env.MODE`